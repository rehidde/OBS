<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel=stylesheet href="/libs/bootstrap/dist/css/bootstrap.min.css">
  <title>Sistem Girişi</title>
  <link rel="stylesheet" href="/css/login.css">
</head>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const toggle = document.getElementById("togglePassword");
    const input = document.getElementById("sifre");

    toggle.addEventListener("click", function () {
      const isHidden = input.type === "password";

      input.type = isHidden ? "text" : "password";

      // şifre açıksa gözün üstü çizilsin
      toggle.classList.toggle("hide", input.type === "text");
    });
  });
</script>

<body>
  <div class="login-container">
    <header class="header">
      <h1>Sistem Giriş Ekranı 🔑</h1>
      <p>Lütfen kullanıcı adı, şifre ve rolünüzü seçerek giriş yapınız.</p>
    </header>

    <form id="loginForm" action="/" method="POST">
      <div class="input-group">
        <label for="id">Kullanıcı Adı:</label>
        <input type="text" id="id" name="id" placeholder="Öğrenci No / Personel ID" required>
      </div>

      <div class="input-group">
        <label for="sifre">Şifre:</label>
        <div class="password-container">
          <input type="password" id="sifre" name="sifre" placeholder="Şifreniz" required>
          <button type="button" id="togglePassword" aria-label="toggle password">👁️</button>
        </div>
      </div>

      <div class="input-group">
        <label for="rol">Rol Seçimi:</label>
        <select id="rol" name="role" required>
          <option value="" disabled selected>Rolünüzü Seçin</option>
          <option value="student">Öğrenci</option>
          <option value="teacher">Öğretmen</option>
        </select>
      </div>

      <button type="submit" id="loginButton">Giriş Yap</button>
    </form>

    <% if (typeof error !== "undefined" && error) { %>
      <div class="message">
        <% if (error === "invalid") { %>
          <p class="error">❌ Kullanıcı adı, şifre veya rol eşleşmedi!</p>
        <% } else if (error === "db_error") { %>
          <p class="warning">⚠️ Sunucu veya Veritabanı Hatası. Lütfen daha sonra tekrar deneyin.</p>
        <% } else { %>
          <p><%= error %></p>
        <% } %>
      </div>
    <% } %>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const toggle = document.getElementById("togglePassword");
      const pass = document.getElementById("password");
      toggle.addEventListener("click", () => {
        pass.type = pass.type === "password" ? "text" : "password";
      });
    });
  </script>
</body>
</html>
